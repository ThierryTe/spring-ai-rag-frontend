services:
  app:
    image: ghcr.io/thierryte/ai-compliance-copilot-frontend:latest
    container_name: ai-rag-frontend
    restart: unless-stopped
    env_file: .env
    ports:
      - "${FRONTEND_PORT:-8081}:80"
